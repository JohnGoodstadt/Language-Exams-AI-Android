package com.goodstadt.john.language.exams.screens.CategoryTab

/**
 * Chinese ships no vocab quizzes, so this stub simply returns null. It exists only so the
 * shared ViewModel code that references SectionQuizKeyMap compiles for the zh flavour.
 */
object SectionQuizKeyMap {
    fun keyFor(level: String, title: String): String? = null
}
